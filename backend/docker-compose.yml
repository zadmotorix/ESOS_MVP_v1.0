version: '3.9'
services:
  api:
    image: node:20
    working_dir: /app
    command: npm run dev
    ports:
      - "3000:3000"
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: esos
      POSTGRES_PASSWORD: esos
      POSTGRES_DB: esos
    ports:
      - "5432:5432"
